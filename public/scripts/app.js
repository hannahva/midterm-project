$(() => {
  // $.ajax({
  //   method: "GET",
  //   url: "/api/users"
  // }).done((users) => {
  //   for(user of users) {
  //     $("<div>").text(user.name).appendTo($("body"));
  //   }
  // });;
  if (id){
    $(".form-group.register").removeClass($(".col-sm-3");
    $("div.main").removeClass($(".col-sm-6 .col-md-8")).addClass($(".col-sm-9 .col-md-10"));
  }
});
